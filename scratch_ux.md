## P2 — accuracy and trust\n\n### 8. Add the free internal-review step for Centrelink (and public housing) — M *(content gate applies)*\n**Problem.** Step 03 of the Centrelink result says \"Lodge with the Administrative Review Tribunal (ART), within the time limit.\" Nothing anywhere in the primary flow mentions asking Services Australia to look at it again — the free, low-fear step that resolves most debts.\n**[verified]** `data/pathways/cth-centrelink.md` models only `avenue.mr = ART`. The substance already exists **as verified content** in `corpus/pathways/cth-centrelink.md` (pathway 1: \"Ask Services Australia to look at the decision again (internal review)\", body \"an Authorised Review Officer (ARO)\", cost free, source servicesaustralia.gov.au) and in `content/faq/ask-centrelink-to-review-a-decision.md`. So this is transcription into the procedural layer, not new research.\n**Change.** Add an optional `internalReview: { body, how, cost, source, verifiedAsAt } | null` block to `DataPathwaySchema`; populate it for `cth-centrelink` and `vic-public-housing` (Housing Appeals Office); render it as the **first** numbered step in `stepsTitle`, ART/VCAT after it. Also add a first framing block to `/learn` (\"Often there is a step before either of these — asking the department to look at it again. It is free, and it is usually quicker\") and soften the \"there are usually two ways\" lead.\n**Files.** `...\\lib\\schemas\\data.ts`, `...\\data\\pathways\\cth-centrelink.md`, `...\\data\\pathways\\vic-public-housing.md`, `...\\components\\feature\\RightsSaverClient.tsx` (`stepsTitle` block), `...\\app\\learn\\page.tsx`, `...\\corpus\\legal\\processes\\merits-review.md`.\n**Gate.** The data layer is a lawyer-sign-off release gate; the new field needs the same verification as the rest of the entry.\n\n### 9. Show \"last checked\", and gate seed entries that are wearing an official source badge — S/M\n**Problem.** The `/start` result never says when anything was last checked, and a seed entry renders a named Act next to an official-looking source link.\n**[verified]** `deadlineRuleView` returns only `{rule, sourceUrl}` and **discards `verifiedAsAt`**. `data/pathways/vic-renting.md` is `status: seed`, `verifiedAsAt: VERIFY`, yet its `deadlineRule` names the *Residential Tenancies Act 1997 (Vic)* and `sourceUrl` is a live vcat.vic.gov.au link — so it renders as a sourced statement. Same for `vic-generic` and `cth-generic`. Meanwhile `/about` promises \"Each entry carries the date it was last checked\" and the home page promises \"Every answer shows where it's from\"; `/ask` and `/decode` *do* show `SourcesPanel` with \"Last checked\".\n**Change.** Return `verifiedAsAt` (and a `stale` boolean from `reviewCadenceDays`) from `deadlineRuleView`. Render \"Last checked: 30 June 2026\" in the amber line. When `verifiedAsAt` is a VERIFY marker or stale, **suppress the Act-specific rule**, fall back to `GENERIC_RULE`, and add \"we are re-checking this — please confirm it with a free service\". Add a `SourcesPanel` to the `/start` result carrying the legal-corpus sources behind the analysis.\n**Files.** `...\\lib\\deadline\\rule.ts`, `...\\components\\feature\\RightsSaverClient.tsx` (the amber time-limit `<p>`, ~L730–750), `...\\app\\about\\page.tsx`.\n\n### 10. Stop the internal forum codes leaking into the downloaded summary — S\n**Problem.** The matter summary a person hands to a legal service says \"Judicial review: **ADJR/FederalCourt**\" / \"**SCV-O56**\".\n**[verified]** The on-screen leak is **already fixed** in the working tree (`planFor` now runs both bodies through `forumName()`), but `lib/handoff/index.ts` L42/L45 still interpolates raw `triage.avenue.mrBody` / `triage.avenue.jrForum`.\n**Change.** Have `buildHandoff` take the resolved `PathPlan[]` from `planFor` (or accept display names), so the download uses the same corpus body names the screen does.\n**Files.** `...\\lib\\handoff\\index.ts`, `...\\components\\feature\\RightsSaverClient.tsx` (`downloadHandoff`).\n\n### 11. Take \"use your own key\" out of user-facing copy — S\n**Problem.** A person in financial hardship reads \"You can use your own key to continue\" as a paywall.\n**[verified]** `lib/cost/guard.ts` L78: \"Please try again tomorrow, or use your own key.\"; L88: \"You can use your own key to continue.\" These are hardcoded English, so they also bypass the reading-level linter.\n**Change.** Move all guard messages into `errors.*` in `en.json`, drop every mention of keys, and pair each with the help block from item 2.\n**Files.** `...\\lib\\cost\\guard.ts` (L54/68/78/88/96), `...\\lib\\i18n\\messages\\en.json`.\n\n---\n\n## P3 — keeping the result, and getting it out of the app\n\n### 12. Don't destroy the person's answers when they leave `/start` — S/M\n**Problem.** The result now links out to FAQ articles, `/learn`, `/help` — all same-tab. Back re-mounts the component at step 1 with everything gone, including the date they dug out of the letter and every ground they ticked.\n**[verified]** `RightsSaverClient` reads `?jur/&area/&date` once in a mount effect and never writes to the URL — no `router.replace`, no `history.pushState/replaceState`, no storage anywhere in the file.\n**Change.** Mirror state into the URL with `history.replaceState` as the user advances (`/start?jur=&area=&date=&step=result`), and extend the existing mount effect to restore to `result` when those params are present. Push one history entry per step so the phone's Back button goes to the previous step instead of exiting. Add a quiet \"Change my answers\" link on the result that returns to step 2 with state intact.\n**Files.** `...\\components\\feature\\RightsSaverClient.tsx`.\n\n### 13. Internal links inside FAQ bodies must open in the same tab — S\n**Problem.** Sixteen of twenty articles contain a body link to `/start`; every one spawns a second tab, Back does nothing, and the user now has two identical-looking tabs.\n**[verified]** `components/ui/Markdown.tsx` L50 renders **every** anchor with `target=\"_blank\" rel=\"noopener noreferrer\"`, unconditionally. Also a WCAG 3.2.5 problem.\n**Change.** Branch in the `a` renderer: `href.startsWith(\"/\")` → `next/link`, same tab, no `target`; external hrefs keep `_blank`.\n**Files.** `...\\components\\ui\\Markdown.tsx`.\n\n### 14. Make print and the draft letters actually usable — M\n**Problem.** Nothing is stored, so print/download is the only way to keep any of this — and printing produces ten pages of stickers with the letter clipped.\n**[verified]** `grep -rn \"@media print\"` across `app/`, `components/`, `lib/` returns **zero**. The reasons letter and the application draft are `<textarea readOnly rows={12}>` / `rows={14}` — browsers print a textarea as its visible box, so most of the letter is lost. `downloadHandoff` calls `a.click()` on a Blob URL with no fallback (silently blocked in Facebook/Instagram/Messenger in-app browsers).\n**Change.** Add an `@media print` block to `globals.css` that prints only the H1, the 01–04 steps, the time-limit line + source, the draft letter and the help list. Auto-grow both textareas to `scrollHeight` on mount (or render as selectable `<pre>`). Add a \"Show the summary on screen\" button beside \"Download\" so it can always be copied or screenshotted.\n**Files.** `...\\app\\globals.css`, `...\\components\\feature\\RightsSaverClient.tsx`.\n\n### 15. Give every request a timeout and the Busy overlay an exit — S/M\n**Problem.** On a phone that drops to one bar mid-upload, the person sits behind an opaque, non-dismissible overlay forever.\n**[verified]** None of `postClassify`, `postAsk`, `postDecodeText`, `postDecodeFile`, `postDeadline`, `postDraft` in `components/feature/api.ts` sets an `AbortSignal` or timeout. `Busy` blocks pointer input, traps focus, ignores Esc, and locks body scroll by design.\n**Change.** `AbortSignal.timeout(30_000)` for text calls, `60_000` for `postDecodeFile`; map `AbortError` to a plain \"that took too long — try again, or get free help\" with links. After ~20s have `Busy` reveal \"Still working — this can be slow on a phone\" plus **Cancel** and **Get free help instead**; Cancel aborts.\n**Files.** `...\\components\\feature\\api.ts`, `...\\components\\ui\\Busy.tsx`.\n\n---\n\n## P4 — first impression, libraries, polish\n\n### 16. Landing hero: name the real situations, get a button on screen 1 — S\n**[verified]** `heroLead` is **46 words**; the foil CTA card is the *second* grid child, so on mobile it lands after all hero text. \"Centrelink\" does not appear until far down the page.\n**Change.** Cut `heroLead` to ~20 words naming real situations (\"A Centrelink debt, a fine, a notice to vacate, a housing refusal — we explain the letter and what you can do\"); move the rest into the coverage band. On mobile, order the foil card above the lead (`order-first lg:order-none`) so a button is on screen 1.\n**Files.** `...\\app\\page.tsx`, `...\\lib\\i18n\\messages\\en.json` (`home.heroLead`).\n\n### 17. Chat: stop showing \"Something went wrong\" for a recoverable limit — S\n**[verified]** `/api/chat` returns `{ok:false, status:\"blocked\", message:\"errors.rateLimited\"}` with **no `reply`**; `useChat` L40 falls back to a hardcoded string, and because `status !== \"unavailable\"` the `ChatPanel` help card with real `/start` and `/help` links never renders.\n**Change.** Branch on `ok === false` in `useChat`: translate `data.message` through `errors.*`, push it as the assistant turn, set `unavailable` so the linked help card renders.\n**Files.** `...\\components\\feature\\chat\\useChat.ts` (L36–48), `...\\app\\api\\chat\\route.ts`, `...\\components\\feature\\chat\\ChatPanel.tsx`.\n\n### 18. Stack the merits-vs-judicial comparison on mobile — S\n**[verified]** `MrVsJr.tsx` L30–31: `<div className=\"overflow-x-auto …\"><table className=\"w-full table-fixed …\">` — `table-fixed` + `w-full` means it never scrolls, it squeezes to ~84px of text per column at 375px. This is the page people open to answer \"which one is mine?\".\n**Change.** Below `sm`, render each row as a stacked block (aspect as a small heading, then \"Merits review: …\" / \"Judicial review: …\"). Keep the table at `sm:` and up. Do **not** just add `min-w-[560px]`.\n**Files.** `...\\components\\feature\\learn\\MrVsJr.tsx`.\n\n### 19. Bring the Learn library inside the reading-level gate — M\n**[verified]** `scripts/reading-level-check.mjs` L44 filters records with `/plainLanguageExplainer|body|answer|description/.test(r.where)`. Every Learn field (`whatItIs`, `whatItMeans`, `oneLine`, `plainExample`, `whatItIsNot`, `whatRelates[i]`, `point[i]`, `row[i].mr`, `chooser[i]`, `element[i].layPrompt`) fails that test — so the entire guide library is ungated.\n**Change.** Widen the regex to include those fields, then rewrite what it flags. Expect `verify` to go red until the copy is fixed — that is the point. Highest offenders to rewrite first: `unreasonableness.whatItMeans`, `jurisdictional-error.w